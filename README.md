#GIT BASH#

git clone

git repo

git add .

git commit -m "Initial message"

git push

%GITHUB%

git init

git add -A

git commit -m "Initial message"

git remote add origin "Repo URL"

git push origin master or main
